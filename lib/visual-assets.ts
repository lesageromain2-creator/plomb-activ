import { gallery, photos } from "./photos";

export const lyonImages = gallery.slice(0, 8);
export const confianceImages = gallery.slice(4, 10);
export const metierGallery = gallery;
export { photos };
